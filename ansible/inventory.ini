# IP comes from `terraform output -raw public_ip` in ../terraform.
# No Elastic IP is attached, so this will change if the instance is ever
# replaced -- regenerate this line after any terraform apply that touches
# aws_instance.devops_server.
[ots_devops]
3.111.187.147 ansible_user=ubuntu
